수학 문제를 생성한다.

사용자가 지정한 학년, 과목, 난이도, 문제 수에 맞춰 problems/ 디렉토리에 YAML 파일을 생성하고, validate → render 순서로 실행하여 output/에 결과물(문제지.docx, 정답지.docx, 미리보기.html)을 만든다.

YAML 작성 시 AGENTS.md의 수식 깨짐 방지 규칙을 반드시 따른다.

$ARGUMENTS
